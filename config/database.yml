default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: tienda_rails_development

test:
  <<: *default
  database: tienda_rails_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>